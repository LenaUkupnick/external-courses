'use strict'
var arr = [1, 2];
var i = 0;
function outputElements (arr) {
    for (i; i < arr.length; i++){
        console.log (arr[i]);
    }
        console.log(arr.length)}

        module.exports = outputElements;
